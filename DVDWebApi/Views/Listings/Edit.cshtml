@model DVDWebApi.Models.Dvd

@{
    ViewBag.Title = "Edit";
}

<div class="row">
    <div class="col-xs-12">
        <h2>Edit DVD</h2>
        @using (Html.BeginForm("Edit", "Listings", FormMethod.Post, new { enctype = "multipart/form-data", id = "frmEditListing" }))
        {
            @Html.ValidationSummary()
            @Html.HiddenFor(x => x.DvdId)
            <div class="col-sm-12 col-md-6">
                <div class="form-group">
                    <label>Title</label>
                    @Html.TextBoxFor(x => x.Title, new { @class = "form-control" })
                </div>
                <div class="form-group">
                    <label>ReleaseYear</label>
                    @Html.TextBoxFor(x => x.ReleaseYear, new { @class = "form-control" })
                </div>
                <div class="form-group">
                    <label>Director</label>
                    @Html.TextBoxFor(x => x.Director, new { @class = "form-control" })
                </div>
                <div class="form-group">
                    <label>Rating</label>
                    @Html.TextBoxFor(x => x.Rating, new { @class = "form-control" })
                </div>
                <div class="form-group">
                    <label>Notes</label>
                    @Html.TextBoxFor(x => x.Notes, new { @class = "form-control" })
                </div>

                <button class="btn btn-primary">Save</button>
            </div>            
        }
    </div>
</div>

@section scripts
{
    <script>
        $(document).ready(function () {
            $('frmEditListing').validate({
                rules: {
                    'Title': {
                        required: true
                    },
                    'ReleaseYear': {
                        required: true,
                        number: true
                    },
                    'Director': {
                        required: true
                    },
                    'Rating': {
                        requried: true
                    },
                    'Notes': {
                        required: true
                    }
                }
            });
        });

    </script>    
}



